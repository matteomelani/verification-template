infrastructure

# Valid values: spike | product | infrastructure
#
# Change this line when the project's mode changes. The LLM reads this file at the start of every session.
# When promoting modes (spike -> product, product -> infrastructure), re-review all contract scorecards.
#
# See .verification/_meta/PROMPT.md for what each mode means.
